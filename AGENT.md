# iiPaintEngine 작업 지침

이 저장소에서 작업하는 에이전트는 아래 규칙을 따른다.

## 답변 원칙

- 모든 답변은 한국어 문어체로 작성한다.
- 문장 종결은 `~다`, `~이다` 말투를 사용한다.
- 작업 결과는 실제 변경 파일, 검증 명령, 남은 위험을 중심으로 간결하게 보고한다.

## 저장소 성격

- 이 저장소는 `iiPaintEngine` 라이브러리 저장소이다.
- 현재 기본 빌드 시스템은 CMake이며, C++ 표준은 C++20이다.
- 루트의 `Brush`, `Canvas`, `Color`, `Stroke` 디렉터리는 페인트 엔진 도메인별 코드 배치 기준으로 사용한다.

## 로컬 라이브러리 및 프레임워크

- 홈 디렉터리의 `.local/`에는 사용자가 개발한 라이브러리 및 프레임워크가 위치한다.
- `.local/` 하위 항목이 소문자로 시작하면 라이브러리로 해석한다.
- `.local/` 하위 항목이 대문자로 시작하면 프레임워크로 해석한다.
- QML을 작성해야 할 때에는 반드시 `.local/LVRS/` 프레임워크를 기준으로 작성한다.

## 개발 방법론

- 기본 개발 방법론은 TDD를 지향한다.
- 기능 추가나 버그 수정 전에는 가능한 한 먼저 실패하는 테스트를 작성한다.
- 기존 동작을 바꾸는 경우에는 회귀 테스트를 먼저 고정한다.
- 소스 코드 변경 및 추가에는 문서와 테스트 갱신이 함께 뒤따라야 한다.
- 소스만 변경하고 테스트 또는 문서를 생략하는 작업은 금한다.

## 빌드 및 검증

- 빌드 디렉터리는 반드시 `build/`를 사용한다.
- 다른 빌드 디렉터리를 새로 만들지 않는다.
- CMake 설정 명령은 다음을 사용한다.

```sh
cmake -S . -B build
```

- 빌드 명령은 다음을 사용한다.

```sh
cmake --build build
```

- 테스트가 구성되어 있다면 다음 명령으로 실행한다.

```sh
ctest --test-dir build --output-on-failure
```

- 매 수정사항 후에는 `build/` 기준으로 빌드를 갱신하고 결과를 확인한다.

## 작업 방식

- 파일 검색은 우선 `rg` 또는 `rg --files`를 사용한다.
- 기존 코드 스타일과 모듈 경계를 먼저 확인한 뒤 변경한다.
- 관련 없는 리팩터링이나 포맷 변경은 피한다.
- 사용자 또는 다른 작업자가 만든 변경사항을 되돌리지 않는다.
- 생성물과 캐시 파일은 소스 변경으로 취급하지 않으며, 필요한 경우에만 갱신한다.
